import 'package:flutter/cupertino.dart';

class ManagerClassWiseFeeDetails extends StatelessWidget {
  const ManagerClassWiseFeeDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: const [],
      ),
    );
  }
}
